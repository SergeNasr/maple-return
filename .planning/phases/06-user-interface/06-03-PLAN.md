---
phase: 06-user-interface
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - maple_return/templates/results.html
  - maple_return/templates/partials/dashboard.html
  - maple_return/templates/partials/amortization_table.html
  - maple_return/templates/partials/pnl_table.html
  - maple_return/templates/partials/exit_summary.html
  - maple_return/templates/partials/scenario_comparison.html
  - maple_return/routes.py
  - maple_return/static/style.css
autonomous: false

must_haves:
  truths:
    - "Dashboard displays key metric cards: equity position, cumulative cashflow, IRR, cap rate, cash-on-cash, ROI"
    - "Dashboard shows USD equivalents for equity and cumulative cashflow"
    - "Dashboard includes a mini chart showing cash flow or equity trend over time"
    - "Amortization table shows monthly rows with date, payment, principal, interest, balance in CAD"
    - "P&L table shows annual rows with revenue, expenses, NOI, mortgage, cashflow, equity metrics"
    - "Scenario comparison shows side-by-side columns for different renewal rate scenarios"
    - "Exit summary shows waterfall breakdown and total return metrics"
    - "Detail tables are accessible via drill-down navigation from the dashboard"
    - "User can edit inputs from results page and see live recalculation"
    - "User can run complete simulation from wizard input to results display in browser"
  artifacts:
    - path: "maple_return/templates/results.html"
      provides: "Main results page with dashboard and drill-down navigation"
      contains: "dashboard"
    - path: "maple_return/templates/partials/dashboard.html"
      provides: "Metric cards and mini chart"
      contains: "metric-card"
    - path: "maple_return/templates/partials/amortization_table.html"
      provides: "Monthly amortization schedule table"
      contains: "data-table"
    - path: "maple_return/templates/partials/pnl_table.html"
      provides: "Annual P&L table"
      contains: "data-table"
    - path: "maple_return/templates/partials/exit_summary.html"
      provides: "Exit waterfall and total return display"
      contains: "net_proceeds"
    - path: "maple_return/templates/partials/scenario_comparison.html"
      provides: "Side-by-side scenario comparison table"
      contains: "scenario"
  key_links:
    - from: "maple_return/templates/results.html"
      to: "/api/simulate"
      via: "HTMX hx-post for live recalculation"
      pattern: "hx-post.*api/simulate"
    - from: "maple_return/routes.py"
      to: "maple_return/templates/results.html"
      via: "Template rendering with simulation data"
      pattern: "results.html"
---

<objective>
Results display page with dashboard, detail tables, and visual verification of the complete flow.

Purpose: Show simulation results as a summary dashboard (metric cards + mini chart) on top, with drill-down into detail tables (amortization, P&L, scenario comparison, exit analysis). Enable live recalculation when inputs change from the results page. Verify the complete wizard-to-results flow works end-to-end.

Output: Complete results page with all tables and dashboard, plus verified end-to-end flow.
</objective>

<execution_context>
@/Users/sergenasr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergenasr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-interface/06-CONTEXT.md
@.planning/phases/06-user-interface/06-01-SUMMARY.md
@.planning/phases/06-user-interface/06-02-SUMMARY.md

# Source files for data structures
@maple_return/analysis.py (PnLRow, AmortizationRow, DashboardSnapshot dataclass fields)
@maple_return/exit_model.py (ExitResult, TotalReturnSummary fields)
@maple_return/mortgage_summary.py (ScenarioComparison fields)
@maple_return/routes.py (simulate endpoint response structure — from plan 06-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create results page with dashboard, detail tables, and drill-down navigation</name>
  <files>
    maple_return/templates/results.html
    maple_return/templates/partials/dashboard.html
    maple_return/templates/partials/amortization_table.html
    maple_return/templates/partials/pnl_table.html
    maple_return/templates/partials/exit_summary.html
    maple_return/templates/partials/scenario_comparison.html
    maple_return/routes.py
    maple_return/static/style.css
  </files>
  <action>
**Add results route** to `maple_return/routes.py`:
- `GET /results` — calls the simulate logic (same as POST /api/simulate but renders HTML instead of JSON). If no saved config or incomplete config, redirect to `/wizard`. On success, render `results.html` with full simulation data in template context.
- Consider refactoring: extract simulation logic from POST /api/simulate into a shared `_run_simulation(config)` helper function that both the API endpoint and the results route can call.

**Create `maple_return/templates/results.html`** extending `base.html`:

Layout structure:
1. **Header bar** with "Maple Return" title and "Edit Inputs" link back to `/wizard`
2. **Dashboard section** — `{% include "partials/dashboard.html" %}`
3. **Tab navigation** — tabs for: "P&L Table", "Amortization", "Scenarios", "Exit Analysis"
4. **Tab content sections** — each tab's content uses `{% include %}` for its partial
5. Only active tab content is visible (JavaScript tab switching)

Tab switching:
- Use CSS classes and JavaScript (not HTMX) since all data is already loaded
- Click tab header to show corresponding content panel
- Active tab gets `.active` class
- Default active tab: "P&L Table"

**Live recalculation:**
- "Edit Inputs" link goes to `/wizard` (user edits there, auto-save persists, then re-runs simulation)
- When user returns from wizard with "Run Simulation", results page re-renders with new data
- Per user decision: "live recalculation on any edit from results page" — implement as a compact edit panel or link back to wizard. Since this is server-rendered HTMX: add a small inline form on results page for quick parameter tweaks (e.g., sale_price, sale_year, cad_per_usd) with `hx-post="/api/simulate"` that swaps the results content. Full edits go back to wizard.

**Create `maple_return/templates/partials/dashboard.html`:**

Metric cards grid (2 rows of 3 cards):
Row 1:
- Equity Position: value in CAD, sub-line in USD
- Cumulative Cash Flow: value in CAD, sub-line in USD
- Hold Period IRR: percentage with 2 decimal places

Row 2:
- Cap Rate: percentage
- Cash-on-Cash Return: percentage
- Simple ROI: percentage (if exit data present)

Each card uses `.metric-card` CSS class with `.label`, `.value`, `.sub` sub-elements.
Negative values get `.negative` class (red text).

**Mini chart:**
- Use a simple inline SVG bar chart or line chart rendered server-side in Jinja2
- Show annual net cash flow trend (positive bars green, negative bars red)
- Or show cumulative equity growth as a line
- No JavaScript charting library needed — use SVG polyline/rect elements calculated from P&L data in template
- Chart container uses `.chart-container` CSS class
- Label: "Annual Net Cash Flow" or "Equity Growth"
- X-axis: years, Y-axis: CAD amounts
- Keep it simple: 5-30 bars/points, no interactivity needed

**Create `maple_return/templates/partials/pnl_table.html`:**

Table with `.data-table` class. Columns:
- Year
- Gross Rent
- Vacancy
- Net Rent
- Operating Expenses
- NOI
- Mortgage Payment
- Net Cash Flow
- Principal Paid
- Interest Paid
- Equity Gained
- Cumulative Cash Flow
- Cumulative Equity
- Cap Rate
- Cash-on-Cash

Format monetary amounts with commas and 2 decimal places using Jinja2 filters. Mark negative values with `.negative` class. Partial year rows get `.partial-year` class.

All amounts in CAD per user decision (USD only in dashboard).

**Create `maple_return/templates/partials/amortization_table.html`:**

Table with `.data-table` class. Columns:
- Date
- Payment
- Principal
- Interest
- Balance

Show all monthly rows. For long schedules (360 months), show first 12 months by default with a "Show all" button that reveals the rest (JavaScript toggle).

All amounts in CAD.

**Create `maple_return/templates/partials/scenario_comparison.html`:**

If multiple scenarios exist, show a comparison table:
- Rows: Renewal Rates, Total Interest, Total Payments, Final Balance, Months to Payoff, Total Equity
- Columns: Scenario 1, Scenario 2, Scenario 3 (side-by-side per user decision)

If only 1 scenario, show a simple summary instead of comparison table.

**Create `maple_return/templates/partials/exit_summary.html`:**

Two sections:

1. **Waterfall breakdown** — vertical flow showing:
   - Sale Price
   - - Commission
   - - Closing Costs
   - - Mortgage Payoff
   - = Net Proceeds (bold)
   - Net Proceeds (USD)
   - Implied Appreciation Rate

2. **Total Return metrics** (if total return data present):
   - Hold Period: X years
   - Cumulative Net Cash Flow
   - Total Profit (CAD and USD)
   - Simple ROI
   - Hold-Period IRR

**Update CSS** in `maple_return/static/style.css`:
- Add any additional styles needed for the mini chart SVG
- Add styles for quick-edit panel on results page
- Ensure all table, card, and tab styles work correctly with real data
- Add print-friendly styles (optional, nice-to-have)
  </action>
  <verify>
Run `just test` to confirm all existing tests pass.
Run `just lint` to confirm no lint errors.
Start server with `just run`:
1. Visit http://localhost:8001/wizard
2. Fill in all 5 steps with test data:
   - Property: date=2020-01-15, price=500000, down=100000
   - Mortgage: rate=0.045, payment=2100, amort=30, type=variable
   - Operating: rent=2500, vacancy=0.05, tax=4000, insurance=1800, maint=2400, mgmt=0.10, rent_esc=0.02, exp_esc=0.02, fx=1.35
   - Scenarios: 1 scenario with 1 renewal term at 0.05 fixed
   - Exit: sale=600000, year=5, commission=0.05, closing=5000
3. Click "Run Simulation"
4. Results page loads with dashboard showing 6 metric cards
5. Mini chart displays annual cash flow or equity trend
6. Tabs work: clicking "Amortization" shows monthly table
7. P&L table shows annual rows with all columns
8. Scenario comparison shows scenario details
9. Exit summary shows waterfall and total return metrics
10. "Edit Inputs" link goes back to wizard with saved values intact
  </verify>
  <done>
Results page displays dashboard with metric cards (equity, cashflow, IRR, cap rate, cash-on-cash, ROI), mini chart, and drill-down tabs for P&L, amortization, scenarios, and exit analysis. All tables show correct data from simulation. Negative values highlighted in red. Tab navigation works. Live recalculation available from results page.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete wizard-to-results flow</name>
  <files>none (verification only)</files>
  <action>
Human verification of the complete web interface for Maple Return property investment analyzer:
- 5-step wizard for all property inputs with auto-save and validation
- Simulation engine orchestrating all calculation modules
- Results dashboard with metric cards and mini chart
- Detail tables (amortization, P&L) with drill-down navigation
- Scenario comparison and exit analysis views
- Live recalculation from results page
  </action>
  <verify>
1. Start the server: `just run`
2. Open browser to http://localhost:8001
3. Verify redirect to wizard page
4. Fill in Step 1 (Property): purchase date, price, down payment
5. Try clicking Next with empty fields — should show validation errors
6. Fill required fields and advance through all 5 steps
7. On Step 5, click "Run Simulation"
8. Verify results page loads with:
   - Dashboard showing metric cards with CAD values
   - USD equivalents shown for equity and cashflow
   - Mini chart visible
   - P&L table with annual data
   - Amortization table with monthly data
   - Scenario comparison (if multiple scenarios entered)
   - Exit analysis with waterfall and total return
9. Close browser, reopen http://localhost:8001/wizard
10. Verify all previously entered values are restored (auto-save)
11. Change a value (e.g., sale price), re-run simulation
12. Verify results update with new values
  </verify>
  <done>Type "approved" if the full flow works correctly, or describe issues to fix</done>
</task>

</tasks>

<verification>
1. `just test` passes — all existing tests pass
2. `just lint` passes — no lint errors
3. Complete flow: wizard input → simulation → results display works end-to-end
4. Dashboard shows all 6 key metrics with correct values
5. Mini chart renders annual trend data
6. All detail tables render with correct columns and formatted data
7. Tab navigation switches between detail views
8. Scenario comparison shows side-by-side columns
9. Exit analysis shows waterfall and total return
10. Auto-save persists wizard state across browser sessions
11. Live recalculation updates results when inputs change
</verification>

<success_criteria>
- Results dashboard displays key metric cards with CAD amounts and USD equivalents
- Mini chart shows annual cash flow or equity trend
- P&L and amortization tables display all required columns with formatted data
- Scenario comparison shows side-by-side columns for different renewal rate assumptions
- Exit analysis shows waterfall breakdown and total return metrics
- Complete flow works: wizard → simulation → results → edit → re-simulate
- Auto-save enables session persistence (close and return)
- Human verification confirms visual correctness and flow functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-interface/06-03-SUMMARY.md`
</output>
