---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - justfile
  - pytest.ini
  - .python-version
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "just lint runs ruff without errors on empty project"
    - "just test runs pytest without errors"
    - "uv manages package dependencies"
    - "Python project structure follows standard conventions"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata and dependencies"
      contains: "name = \"maple-return\""
    - path: "justfile"
      provides: "Task automation commands"
      exports: ["lint", "test", "run"]
    - path: "pytest.ini"
      provides: "Test configuration"
      contains: "[pytest]"
  key_links:
    - from: "justfile"
      to: "uv"
      via: "package management commands"
      pattern: "uv (sync|add|run)"
    - from: "justfile"
      to: "ruff"
      via: "lint command"
      pattern: "ruff check"
    - from: "justfile"
      to: "pytest"
      via: "test command"
      pattern: "pytest"
---

<objective>
Set up Python project infrastructure with uv package management, ruff linting, and pytest testing. This establishes the foundational tooling for all subsequent development.

Purpose: Create a properly configured Python project using the owner's preferred toolchain
Output: Working project with lint, test, and run commands
</objective>

<execution_context>
@/Users/sergenasr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergenasr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create Python project with uv and configure dependencies</name>
  <files>
pyproject.toml
.python-version
.gitignore
  </files>
  <action>
Initialize a Python project using uv:
- Run `uv init --name maple-return --no-readme` to create project structure
- Set Python version to 3.12 in .python-version (modern, stable)
- Configure pyproject.toml:
  - Package name: "maple-return" (matches project)
  - Add dependencies: fastapi, uvicorn[standard], jinja2, aiosqlite, python-multipart
  - Add dev dependencies: pytest, pytest-asyncio, ruff, httpx (for testing FastAPI)
  - Set requires-python = ">=3.12"
- Update .gitignore to include:
  - __pycache__/
  - *.py[cod]
  - .pytest_cache/
  - .ruff_cache/
  - .venv/
  - *.db
  - *.db-journal
- Run `uv sync` to install dependencies and create virtual environment
  </action>
  <verify>
Run `uv sync` and verify it completes without errors. Check that .venv directory is created and pyproject.toml has all specified dependencies.
  </verify>
  <done>
pyproject.toml exists with correct package name and all required dependencies. Virtual environment is created and dependencies are installed.
  </done>
</task>

<task type="auto">
  <name>Create Justfile with standard commands</name>
  <files>
justfile
  </files>
  <action>
Create a justfile with these commands:

```just
# Run ruff linter
lint:
    uv run ruff check .

# Run ruff linter with auto-fix
lint-fix:
    uv run ruff check --fix .

# Run pytest tests
test:
    uv run pytest

# Run FastAPI dev server
run:
    uv run uvicorn maple_return.main:app --reload --host 127.0.0.1 --port 8000

# Install/sync dependencies
install:
    uv sync
```

Use tabs for indentation (justfile requires tabs). Keep commands simple and delegate to uv for all Python execution.
  </action>
  <verify>
Run `just --list` to verify all commands are defined. Run `just lint` (should pass on empty project).
  </verify>
  <done>
justfile exists with lint, test, run, and install commands. All commands execute without syntax errors.
  </done>
</task>

<task type="auto">
  <name>Configure pytest and ruff</name>
  <files>
pytest.ini
pyproject.toml
  </files>
  <action>
Create pytest.ini:
```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
```

Add ruff configuration to pyproject.toml:
```toml
[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = []

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
```

Create empty tests directory and a placeholder test file tests/test_placeholder.py:
```python
def test_placeholder():
    """Placeholder test to verify pytest works."""
    assert True
```

This ensures `just test` passes even with no real tests yet.
  </action>
  <verify>
Run `just test` and verify pytest finds and passes the placeholder test. Run `just lint` and verify ruff runs without errors.
  </verify>
  <done>
pytest.ini exists with correct configuration. Ruff is configured in pyproject.toml. Placeholder test passes. Both `just test` and `just lint` execute successfully.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `just install` to verify uv dependency management works
2. Run `just lint` to verify ruff linting works
3. Run `just test` to verify pytest works
4. Check that pyproject.toml has all required dependencies
5. Verify .venv directory exists with installed packages
</verification>

<success_criteria>
- Python 3.12 project exists with uv package management
- pyproject.toml defines project metadata and all required dependencies
- justfile provides lint, test, run, and install commands
- pytest runs and passes with placeholder test
- ruff runs without errors
- Virtual environment is created and dependencies are installed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
