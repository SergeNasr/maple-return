---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - maple_return/main.py
  - maple_return/database.py
  - maple_return/templates/base.html
  - maple_return/templates/index.html
  - maple_return/static/style.css
  - alembic.ini
  - alembic/env.py
  - alembic/versions/.gitkeep
autonomous: true

must_haves:
  truths:
    - "FastAPI server starts and responds to requests"
    - "SQLite database is initialized and accessible"
    - "Templates render HTML pages correctly"
    - "Test page displays in browser at http://localhost:8000"
    - "Static CSS is served and applied"
  artifacts:
    - path: "maple_return/main.py"
      provides: "FastAPI application and routes"
      exports: ["app"]
      min_lines: 40
    - path: "maple_return/database.py"
      provides: "SQLite database setup and session management"
      exports: ["engine", "get_db"]
      min_lines: 20
    - path: "maple_return/templates/base.html"
      provides: "Base HTML template"
      min_lines: 20
    - path: "maple_return/templates/index.html"
      provides: "Test page template"
      min_lines: 15
  key_links:
    - from: "maple_return/main.py"
      to: "maple_return/database.py"
      via: "database session dependency"
      pattern: "from .+database import get_db"
    - from: "maple_return/main.py"
      to: "templates/*.html"
      via: "Jinja2 template rendering"
      pattern: "templates\\.TemplateResponse"
---

<objective>
Set up FastAPI web server with SQLite database, Jinja2 template rendering, and a test page. This creates the web foundation that subsequent phases will build upon.

Purpose: Establish working web server with database and basic page rendering
Output: Running web application serving a test page at localhost:8000
</objective>

<execution_context>
@/Users/sergenasr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergenasr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Set up SQLite database with async support</name>
  <files>
maple_return/database.py
pyproject.toml
  </files>
  <action>
Create maple_return/database.py with SQLAlchemy async SQLite setup:

- Import: `from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker`
- Import: `from sqlalchemy.orm import declarative_base`
- Database URL: `sqlite+aiosqlite:///./maple_return.db` (local file in project root)
- Create async engine with `create_async_engine(DATABASE_URL, echo=True)`
- Create async session maker with `async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)`
- Create declarative Base: `Base = declarative_base()`
- Define async dependency function `get_db()` that yields session
- Export: engine, Base, get_db

Ensure pyproject.toml has sqlalchemy[asyncio] and aiosqlite dependencies (add if missing from Plan 01).

Note: Per user's decision in CONTEXT.md, using SQLite for data persistence, storing only inputs and computing results on the fly.
  </action>
  <verify>
Run `just lint` to verify no linting errors. Import database module: `from maple_return.database import engine, Base, get_db` and verify no import errors.
  </verify>
  <done>
database.py exists with async SQLite setup using aiosqlite. Engine, Base, and get_db dependency are defined and importable. Database file will be created when server starts.
  </done>
</task>

<task type="auto">
  <name>Create FastAPI application with template rendering</name>
  <files>
maple_return/main.py
maple_return/templates/base.html
maple_return/templates/index.html
maple_return/static/style.css
  </files>
  <action>
Create maple_return/main.py with FastAPI application:

**FastAPI setup:**
- Import FastAPI, Request
- Import Jinja2Templates from fastapi.templating
- Import StaticFiles from fastapi.staticfiles
- Create app: `app = FastAPI(title="Maple Return")`
- Configure templates: `templates = Jinja2Templates(directory="maple_return/templates")`
- Mount static files: `app.mount("/static", StaticFiles(directory="maple_return/static"), name="static")`

**Create startup event:**
- Use @app.on_event("startup") to create database tables on startup
- Import Base and engine from database module
- Run `async with engine.begin() as conn: await conn.run_sync(Base.metadata.create_all)`

**Create test route:**
- GET "/" route that renders index.html template
- Pass context with page title and test message
- Use TemplateResponse to render template

**Create templates/base.html:**
- Basic HTML5 structure with <!DOCTYPE html>
- Include <title> block for page titles
- Include <link> to /static/style.css
- Include {% block content %} for page content
- Keep it simple and clean

**Create templates/index.html:**
- Extend base.html
- Override title block with "Maple Return - Test Page"
- Override content block with heading "Maple Return" and test message
- Add paragraph stating "Foundation phase: Web server is running successfully"

**Create static/style.css:**
- Simple, clean styles: body font (sans-serif), centered layout, basic spacing
- Style for main heading (h1) with color
- Container max-width and padding
- Keep it minimal and functional per user's "lightweight feel" preference in CONTEXT.md

Note: Per user's decision in CONTEXT.md, using lightweight HTML rendering approach (not a JS SPA). htmx or similar will be added in later phases when interactivity is needed.
  </action>
  <verify>
Run `just run` to start the server. Visit http://localhost:8000 in browser and verify:
1. Page loads without errors
2. Title displays "Maple Return - Test Page"
3. Heading shows "Maple Return"
4. Test message is visible
5. CSS styles are applied (check font and colors)
Stop server with Ctrl+C.
  </verify>
  <done>
FastAPI application runs and serves test page at localhost:8000. Templates render correctly with base template and CSS styling. Database initializes on startup. Static files are served correctly.
  </done>
</task>

<task type="auto">
  <name>Add database dependencies and create basic test for web server</name>
  <files>
pyproject.toml
tests/test_main.py
  </files>
  <action>
Update pyproject.toml dependencies if needed (should already have from Plan 01, verify):
- sqlalchemy[asyncio] (for async SQLAlchemy)
- aiosqlite (async SQLite driver)
- alembic (database migrations - add as dev dependency)

Create tests/test_main.py with basic FastAPI tests:

**Test home route:**
- Use httpx.AsyncClient to test async FastAPI app
- Test GET "/" returns 200 status
- Test response contains "Maple Return" text
- Use `async with AsyncClient(app=app, base_url="http://test") as client`

Import: `from fastapi.testclient import TestClient` or use httpx directly for async
Import: `from maple_return.main import app`

Use pytest-asyncio for async tests if needed (should be in dev dependencies from Plan 01).
  </action>
  <verify>
Run `just test` and verify web server tests pass. Verify database dependencies are in pyproject.toml. Run `uv sync` to ensure all dependencies are installed.
  </verify>
  <done>
pyproject.toml includes all database dependencies. tests/test_main.py exists with tests for home route. All tests pass. Dependencies are synchronized.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `just run` to start the server
2. Visit http://localhost:8000 and verify test page loads
3. Verify CSS styles are applied
4. Check that maple_return.db file is created
5. Run `just test` and verify all tests pass
6. Stop server and verify clean shutdown
</verification>

<success_criteria>
- FastAPI application is configured and runs
- SQLite database is initialized on startup
- Jinja2 templates render HTML pages
- Static CSS files are served correctly
- Test page displays at http://localhost:8000 with proper styling
- Database file is created when server starts
- Tests pass for web server routes
- `just run` starts the development server successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
