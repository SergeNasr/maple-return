---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - maple_return/models.py
  - tests/test_models.py
autonomous: true

must_haves:
  truths:
    - "Property model captures purchase details"
    - "Mortgage model captures loan details and term structure"
    - "CashFlow model captures income and expense tracking"
    - "Models have proper type hints and validation"
  artifacts:
    - path: "maple_return/models.py"
      provides: "Core data models"
      exports: ["Property", "Mortgage", "CashFlow"]
      min_lines: 60
    - path: "tests/test_models.py"
      provides: "Model validation tests"
      min_lines: 30
  key_links:
    - from: "tests/test_models.py"
      to: "maple_return.models"
      via: "imports and instantiation"
      pattern: "from maple_return\\.models import"
---

<objective>
Define core data models (Property, Mortgage, CashFlow) using Python dataclasses. These models represent the domain objects that store user inputs and structure the data for calculations.

Purpose: Create type-safe, validated data models for property investment inputs
Output: Python data models with tests
</objective>

<execution_context>
@/Users/sergenasr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergenasr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create maple_return package with data models</name>
  <files>
maple_return/__init__.py
maple_return/models.py
  </files>
  <action>
Create the maple_return package directory and define core data models in models.py using Python dataclasses:

**Property model:**
- purchase_date: date (when property was acquired)
- purchase_price: Decimal (in CAD)
- down_payment: Decimal (in CAD)
- monthly_rent: Decimal (in CAD)
- vacancy_rate: Decimal (0.0 to 1.0, e.g., 0.05 for 5%)
- property_tax_annual: Decimal (in CAD)
- insurance_annual: Decimal (in CAD)
- maintenance_annual: Decimal (in CAD)
- rent_escalation_rate: Decimal (annual %, e.g., 0.02 for 2%)
- expense_escalation_rate: Decimal (annual %)

**Mortgage model:**
- principal: Decimal (initial loan amount in CAD)
- rate: Decimal (current interest rate, e.g., 0.045 for 4.5%)
- payment_amount: Decimal (monthly payment in CAD)
- amortization_years: int (default 30)
- term_years: int (default 5 for Canadian mortgages)
- is_variable: bool (True for variable rate, False for fixed)

**CashFlow model:**
- month: date (first day of month)
- rent_income: Decimal (in CAD)
- property_tax: Decimal (monthly portion)
- insurance: Decimal (monthly portion)
- maintenance: Decimal (monthly portion)
- mortgage_payment: Decimal
- mortgage_principal: Decimal (principal portion)
- mortgage_interest: Decimal (interest portion)
- net_cashflow: Decimal (total income - total expenses)

Use Decimal type for all monetary amounts (from decimal import Decimal). Use date for all date fields (from datetime import date). Add proper type hints. Add __init__.py to make maple_return a package.
  </action>
  <verify>
Run `just lint` to verify no linting errors. Import models in Python REPL: `from maple_return.models import Property, Mortgage, CashFlow` and verify no import errors.
  </verify>
  <done>
maple_return package exists with models.py defining Property, Mortgage, and CashFlow dataclasses. All fields have proper type hints using Decimal and date types. Code passes ruff linting.
  </done>
</task>

<task type="auto">
  <name>Create tests for data models</name>
  <files>
tests/test_models.py
  </files>
  <action>
Create tests/test_models.py with basic validation tests:

**Test Property model:**
- test_property_creation: Create Property with valid data, verify all fields
- test_property_types: Verify purchase_price and down_payment are Decimal
- test_property_date: Verify purchase_date is a date object

**Test Mortgage model:**
- test_mortgage_creation: Create Mortgage with valid data
- test_mortgage_defaults: Verify default amortization_years=30, term_years=5
- test_mortgage_types: Verify principal, rate, payment_amount are Decimal

**Test CashFlow model:**
- test_cashflow_creation: Create CashFlow with valid data
- test_cashflow_calculations: Create CashFlow and verify net_cashflow matches expected value

Import models: `from maple_return.models import Property, Mortgage, CashFlow`
Import required types: `from decimal import Decimal` and `from datetime import date`

Use pytest conventions: test functions start with test_, use assert for validation.
  </action>
  <verify>
Run `just test` and verify all model tests pass. Verify test coverage includes all three models.
  </verify>
  <done>
tests/test_models.py exists with tests for Property, Mortgage, and CashFlow models. All tests pass. Tests verify model creation, field types, and basic validation.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `just lint` and verify no errors on models.py
2. Run `just test` and verify all model tests pass
3. Import models in Python REPL to verify they're accessible
4. Verify all models use Decimal for monetary amounts and date for dates
</verification>

<success_criteria>
- maple_return package exists with models.py
- Property, Mortgage, and CashFlow dataclasses are defined
- All monetary fields use Decimal type
- All date fields use date type
- Models have proper type hints
- Tests exist and pass for all three models
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
